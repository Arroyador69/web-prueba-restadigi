{
  "name": "web-prueba-restadigi",
  "version": "2.0.0",
  "private": true,
  "description": "Demo pública Restadigi (landing + dashboard oficial sin login)",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm run build --prefix restadigi-demo",
    "dev": "npm run dev --prefix restadigi-demo",
    "db:init": "npm run db:init --prefix restadigi-demo"
  }
}
